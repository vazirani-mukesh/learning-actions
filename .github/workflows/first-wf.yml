name: First WorkFlow

on:
  issue_comment:
    types:
      - created
      - edited
      - deleted
  

jobs:
  my-job:
    runs-on: ubuntu-latest
    #if: ${{ github.event.client_payload.environment == 'dev' }}
    steps:
      - name: Checkout Dir
        uses: actions/checkout@v3
      - name: Python Shell
        run: print('Hello World!!!')
        shell: python
      - name: PWSH Shell
        run: Get-ChildItem
        shell: pwsh
      - name: Print Payload Varibales
        run: echo ${{ github.event.client_payload.environment }}
        shell: bash
